@model GiftOfTheGivers.Models.ProjectUpdate

@{
    ViewData["Title"] = "Edit Project Update";
}

<div class="container my-5">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <h1 class="display-4 mb-4">Edit Project Update</h1>
            <p class="lead">Project: <strong>@ViewBag.ProjectTitle</strong></p>

            <div class="card shadow">
                <div class="card-body">
                    <form asp-action="EditProjectUpdate" method="post">
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="ReliefProjectId" />
                        <input type="hidden" asp-for="CreatedBy" />
                        <input type="hidden" asp-for="CreatedDate" />

                        <div class="mb-3">
                            <label asp-for="Title" class="form-label"></label>
                            <input asp-for="Title" class="form-control" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Content" class="form-label"></label>
                            <textarea asp-for="Content" class="form-control" rows="8"></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ImageUrl" class="form-label"></label>
                            <input asp-for="ImageUrl" class="form-control" type="url" />
                            <small class="form-text text-muted">Optional: Enter a URL for an image related to this update</small>
                            <span asp-validation-for="ImageUrl" class="text-danger"></span>
                        </div>

                        <div class="alert alert-info">
                            <small>
                                <strong>Created by:</strong> @Model.CreatedBy on @Model.CreatedDate.ToString("yyyy-MM-dd hh:mm tt")
                            </small>
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-primary btn-lg">Save Changes</button>
                            <a asp-action="ReliefProjects" class="btn btn-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
